{
  "name": "@lifeos/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:format": "prisma format",
    "db:validate": "prisma validate",
    "db:generate": "prisma generate",
    "db:migrate:dev": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:status": "prisma migrate status",
    "db:seed": "prisma db seed",
    "db:sqlite:validate": "prisma validate --config prisma.sqlite.config.ts",
    "db:sqlite:generate": "prisma generate --config prisma.sqlite.config.ts",
    "db:sqlite:migrate": "node --import tsx prisma/sqlite/migrate-cli.ts",
    "db:sqlite:seed": "prisma db seed --config prisma.sqlite.config.ts",
    "db:sqlite:test": "npm run db:sqlite:generate && node --import tsx --test tests/sqlite-migration.integration.test.ts",
    "pretest": "prisma generate && prisma generate --config prisma.sqlite.config.ts",
    "test": "tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/adapter-better-sqlite3": "7.8.0",
    "@prisma/adapter-pg": "7.8.0",
    "@prisma/client": "^7.8.0",
    "better-sqlite3": "12.11.1",
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.20.1",
    "@types/pg": "^8.20.0",
    "prisma": "^7.8.0",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
