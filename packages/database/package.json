{
  "name": "@lifeos/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:format": "prisma format",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  },
  "devDependencies": {
    "prisma": "^6.14.0"
  }
}
